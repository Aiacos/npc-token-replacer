---
phase: 05-progress-bar
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - scripts/lib/progress-reporter.js
  - tests/lib/progress-reporter.test.js
autonomous: true
requirements:
  - UX-01

must_haves:
  truths:
    - "ProgressReporter.start() creates a v13 progress notification when ui.notifications.update exists"
    - "ProgressReporter.start() calls SceneNavigation.displayProgressBar when v13 API is unavailable"
    - "ProgressReporter.update() advances v13 notification with 0.0-1.0 fraction"
    - "ProgressReporter.update() advances v12 displayProgressBar with 0-100 integer"
    - "ProgressReporter.finish() sets progress to 100% on both paths"
  artifacts:
    - path: "scripts/lib/progress-reporter.js"
      provides: "ProgressReporter class with start/update/finish and v12/v13 duck-typing"
      exports: ["ProgressReporter"]
    - path: "tests/lib/progress-reporter.test.js"
      provides: "Unit tests for v13 and v12 code paths"
      min_lines: 60
  key_links:
    - from: "scripts/lib/progress-reporter.js"
      to: "ui.notifications.info"
      via: "v13 progress notification API"
      pattern: "ui\\.notifications\\.info.*progress.*true"
    - from: "scripts/lib/progress-reporter.js"
      to: "SceneNavigation.displayProgressBar"
      via: "v12 fallback"
      pattern: "SceneNavigation\\.displayProgressBar"
---

<objective>
Create the ProgressReporter class with TDD, covering both Foundry v12 and v13 progress bar APIs behind a unified interface.

Purpose: This class is the core abstraction for UX-01. It must correctly handle the different pct scales (v12: 0-100, v13: 0.0-1.0) and detection mechanisms. TDD ensures both code paths are exercised before the class is wired into the controller.

Output: `scripts/lib/progress-reporter.js` with exported ProgressReporter class, `tests/lib/progress-reporter.test.js` with passing tests for both version paths.
</objective>

<execution_context>
@/home/aiacos/.claude/get-shit-done/workflows/execute-plan.md
@/home/aiacos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-progress-bar/05-RESEARCH.md

@scripts/lib/logger.js
@scripts/lib/wildcard-resolver.js
@vitest.config.js
@tests/setup/foundry-mocks.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - Write failing tests for ProgressReporter</name>
  <files>tests/lib/progress-reporter.test.js, scripts/lib/progress-reporter.js</files>
  <behavior>
    - v13 detection: when `typeof ui.notifications.update === "function"`, ProgressReporter uses v13 path
    - v12 detection: when `ui.notifications.update` is undefined, ProgressReporter uses v12 SceneNavigation fallback
    - start(total, label): v13 calls `ui.notifications.info(label, { progress: true })` and stores returned notification; v12 calls `SceneNavigation.displayProgressBar({ label, pct: 0 })`
    - update(current, label): v13 calls `notification.update({ pct: current/total, message: label })` with fraction 0.0-1.0; v12 calls `SceneNavigation.displayProgressBar({ label, pct: Math.round((current/total)*100) })` with integer 0-100
    - finish(): v13 calls `notification.update({ pct: 1.0, message: label })` and clears reference; v12 calls `SceneNavigation.displayProgressBar({ label, pct: 100 })`
    - Edge case: update() clamps pct to max 1.0 (v13) / 100 (v12) even if current > total
  </behavior>
  <action>
    Create `scripts/lib/progress-reporter.js` as a minimal stub that exports the ProgressReporter class with `start()`, `update()`, and `finish()` methods that throw `new Error("Not implemented")`.

    Create `tests/lib/progress-reporter.test.js` with two describe blocks:

    1. "v13 path (notifications progress API available)" — beforeEach sets up:
       - `ui.notifications.update = vi.fn()` (detection signal)
       - `ui.notifications.info = vi.fn(() => ({ update: vi.fn() }))` (returns notification object)
       Tests:
       - start() calls ui.notifications.info with progress: true
       - update(3, "label") calls notification.update with pct close to 0.3 (3/10) and message "label"
       - finish() calls notification.update with pct 1.0
       - finish() clears internal notification reference (subsequent update is no-op)

    2. "v12 path (SceneNavigation fallback)" — beforeEach sets up:
       - `delete ui.notifications.update` (remove v13 signal)
       - `globalThis.SceneNavigation = { displayProgressBar: vi.fn() }`
       Tests:
       - start() calls SceneNavigation.displayProgressBar with pct: 0
       - update(3, "label") calls SceneNavigation.displayProgressBar with pct: 30 (integer)
       - finish() calls SceneNavigation.displayProgressBar with pct: 100

    Follow project test conventions: import from vitest, use beforeEach for mock setup, clean up with afterEach/restoreAllMocks.

    Run tests — they MUST fail (RED phase). Commit: `test(05-01): add failing tests for ProgressReporter v12/v13 paths`
  </action>
  <verify>
    <automated>cd /home/aiacos/workspace/FoundryVTT/token_updater && npx vitest run tests/lib/progress-reporter.test.js 2>&1 | tail -20</automated>
  </verify>
  <done>Test file exists with 7+ test cases covering both v12 and v13 paths. All tests fail because ProgressReporter methods throw "Not implemented".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN + REFACTOR - Implement ProgressReporter</name>
  <files>scripts/lib/progress-reporter.js</files>
  <action>
    Implement the ProgressReporter class in `scripts/lib/progress-reporter.js`:

    ```javascript
    import { Logger } from "./logger.js";
    ```

    Class design (instance-based, NOT static — needs per-instance state for #notification and #total):
    - Private instance fields: `#notification` (null), `#total` (0)
    - Static private method `#isV13ProgressAvailable()` — returns `typeof ui.notifications?.update === "function"` (duck-typing per project convention, NOT game.version check)
    - `start(total, label)` — stores total. If v13: calls `ui.notifications.info(label, { progress: true })` and stores result in `#notification`. If v12: calls `SceneNavigation.displayProgressBar({ label, pct: 0 })`.
    - `update(current, label)` — computes `pct = Math.min(current / this.#total, 1)`. If `#notification` exists: calls `this.#notification.update({ pct, message: label })` (v13 fraction). Else: calls `SceneNavigation.displayProgressBar({ label, pct: Math.round(pct * 100) })` (v12 integer).
    - `finish()` — If `#notification`: calls `this.#notification.update({ pct: 1.0 })`, then sets `this.#notification = null`. Else: calls `SceneNavigation.displayProgressBar({ label: "", pct: 100 })`. Resets `#total = 0`.

    Key details:
    - Use `Math.min(current / this.#total, 1)` to clamp — avoids >100% if current exceeds total
    - Do NOT pass a finish label message to the progress bar — the existing `#reportResults()` handles the summary notification separately. The progress bar auto-dismisses at 100% in v13.
    - Export: `export { ProgressReporter }`

    Run all tests. They MUST pass. Commit: `feat(05-01): implement ProgressReporter with v12/v13 dual path`

    REFACTOR if needed (e.g., extract detection to a readable helper). Run tests again after any refactor. Commit if changed: `refactor(05-01): clean up ProgressReporter`
  </action>
  <verify>
    <automated>cd /home/aiacos/workspace/FoundryVTT/token_updater && npx vitest run tests/lib/progress-reporter.test.js && npx vitest run</automated>
  </verify>
  <done>All ProgressReporter tests pass (GREEN). All existing tests still pass (no regressions). ProgressReporter exports correctly from scripts/lib/progress-reporter.js.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/lib/progress-reporter.test.js` — all ProgressReporter tests pass
- `npx vitest run` — all existing tests still pass (no regressions)
- `scripts/lib/progress-reporter.js` exports ProgressReporter class
- v13 tests verify `ui.notifications.info` called with `{ progress: true }`
- v12 tests verify `SceneNavigation.displayProgressBar` called with integer pct
- pct scale difference is correctly handled (0.0-1.0 vs 0-100)
</verification>

<success_criteria>
- ProgressReporter class exists in scripts/lib/progress-reporter.js with start(), update(), finish()
- Tests cover v13 notification path and v12 SceneNavigation fallback path
- v13 uses fraction 0.0-1.0, v12 uses integer 0-100
- Duck-typing detection (not game.version) used per project convention
- All tests pass including existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/05-progress-bar/05-01-SUMMARY.md`
</output>
