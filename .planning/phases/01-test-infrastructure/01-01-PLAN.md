---
phase: 01-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.js
  - .gitignore
  - tests/setup/foundry-mocks.js
  - tests/setup/cache-clearing.js
  - tests/smoke.test.js
autonomous: true
requirements:
  - TEST-01
  - TEST-06
must_haves:
  truths:
    - "Running `npm test` from project root exits with code 0 and produces Vitest output"
    - "Vitest is configured with jsdom environment, unstubGlobals true, and foundry-test-utils as setupFiles"
    - "A beforeEach cache-clearing template exists that other test files can follow"
    - "Coverage infrastructure is configured and ready for Phase 3 to use"
    - "A smoke test validates foundry-test-utils Foundry globals are actually stubbed"
  artifacts:
    - path: "package.json"
      provides: "Test scripts, devDependencies, ESM type declaration"
      contains: "vitest run"
    - path: "vitest.config.js"
      provides: "Vitest configuration with jsdom, unstubGlobals, passWithNoTests, setupFiles, coverage"
      contains: "unstubGlobals"
    - path: ".gitignore"
      provides: "Excludes coverage/ directory from git"
      contains: "coverage/"
    - path: "tests/setup/foundry-mocks.js"
      provides: "Project-specific Foundry mock extensions for globals not in foundry-test-utils"
      contains: "game.packs"
    - path: "tests/setup/cache-clearing.js"
      provides: "Template beforeEach pattern for cache clearing"
      contains: "beforeEach"
    - path: "tests/smoke.test.js"
      provides: "Smoke test proving infrastructure works"
      contains: "describe"
  key_links:
    - from: "vitest.config.js"
      to: "@rayners/foundry-test-utils/dist/helpers/setup.js"
      via: "setupFiles array"
      pattern: "foundry-test-utils.*setup"
    - from: "vitest.config.js"
      to: "tests/setup/foundry-mocks.js"
      via: "setupFiles array"
      pattern: "foundry-mocks"
    - from: "package.json"
      to: "vitest.config.js"
      via: "npm test -> vitest run"
      pattern: "vitest run"
    - from: "tests/smoke.test.js"
      to: "tests/setup/foundry-mocks.js"
      via: "Vitest setupFiles runs before test"
      pattern: "game.*packs"
---

<objective>
Configure Vitest + foundry-test-utils so `npm test` runs clean with zero errors. Install all test dependencies, create the Vitest configuration, project-specific Foundry mock extensions, a beforeEach cache-clearing template, and a smoke test that proves the infrastructure works.

Purpose: Establishes the structural foundation all subsequent test phases depend on. No unit tests are written -- just the infrastructure that makes writing them possible.
Output: Working test runner (`npm test` exits 0), configured coverage infrastructure, documented mock gaps for Phase 2/3.
</objective>

<execution_context>
@/home/aiacos/.claude/get-shit-done/workflows/execute-plan.md
@/home/aiacos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-infrastructure/01-CONTEXT.md
@.planning/phases/01-test-infrastructure/01-RESEARCH.md

<interfaces>
<!-- No existing interfaces to extract -- this plan creates new files from scratch. -->
<!-- Key constraint: scripts/main.js uses ES2020 private static fields (#field). -->
<!-- Vitest 3.2.4 handles these natively -- no transpilation needed. -->

Existing clearCache() methods available for beforeEach template (from CLAUDE.md):
- CompendiumManager.clearCache()    // clears #indexCache, #wotcCompendiumsCache
- FolderManager.clearCache()         // clears #importFolderCache
- WildcardResolver.clearCache()      // clears #variantCache
- TokenReplacer.resetCounter()       // resets #sequentialCounter
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install test dependencies and configure Vitest</name>
  <files>package.json, vitest.config.js, .gitignore</files>
  <action>
1. Update package.json:
   - Change `"name"` from `"001-ricontrolla-il-progetto-correggi-gli-errori-rendil"` to `"npc-token-replacer"` (research pitfall 7)
   - Add `"type": "module"` for native ESM support
   - Replace `scripts.test` with `"vitest run"`
   - Add `"test:watch": "vitest"` and `"test:coverage": "vitest run --coverage"`
   - Add devDependencies: `"vitest": "^3.1.0"`, `"@rayners/foundry-test-utils": "^1.2.2"`, `"@vitest/coverage-v8": "^3.1.0"`, `"jsdom": "^27.0.0"`
   - Keep existing eslint devDependency

2. Create vitest.config.js at project root using the research-verified configuration:
   ```javascript
   import { defineConfig } from "vitest/config";

   export default defineConfig({
     test: {
       environment: "jsdom",
       globals: true,
       unstubGlobals: true,
       passWithNoTests: true,
       setupFiles: [
         "@rayners/foundry-test-utils/dist/helpers/setup.js",
         "./tests/setup/foundry-mocks.js"
       ],
       include: ["tests/**/*.test.js"],
       coverage: {
         provider: "v8",
         reporter: ["text", "html"],
         include: ["scripts/**/*.js"]
       }
     }
   });
   ```

   CRITICAL version constraint: Use Vitest 3.x (^3.1.0), NOT Vitest 4.x. foundry-test-utils@1.2.2 has peer dep `vitest: "^3.1.0"` which caps at 3.x.
   CRITICAL path: Use `@rayners/foundry-test-utils/dist/helpers/setup.js` (not `helpers/setup.js`). The README path does not resolve correctly.

3. Append `coverage/` to .gitignore (after existing entries, with a comment header `# Test coverage output`).

4. Run `npm install` to install all dependencies and generate package-lock.json.
  </action>
  <verify>
    <automated>cd /home/aiacos/workspace/FoundryVTT/token_updater && node -e "const pkg = require('./package.json'); console.assert(pkg.name === 'npc-token-replacer'); console.assert(pkg.type === 'module'); console.assert(pkg.scripts.test === 'vitest run'); console.assert(pkg.devDependencies.vitest); console.assert(pkg.devDependencies['@rayners/foundry-test-utils']); console.log('package.json OK')" && test -f vitest.config.js && grep -q 'unstubGlobals' vitest.config.js && grep -q 'coverage/' .gitignore && echo "All config files OK"</automated>
  </verify>
  <done>package.json has correct name, type:module, test scripts, and all 4 devDependencies. vitest.config.js exists with jsdom, unstubGlobals, passWithNoTests, foundry-test-utils setupFile, coverage v8 provider. .gitignore includes coverage/. npm install completed without peer dependency errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create test setup files and smoke test</name>
  <files>tests/setup/foundry-mocks.js, tests/setup/cache-clearing.js, tests/smoke.test.js</files>
  <action>
1. Create `tests/setup/foundry-mocks.js` -- project-specific Foundry mock extensions:
   - Add `game.packs` mock with `get`, `filter`, `forEach`, `size` (Collection-like interface CompendiumManager expects). Use `vi.fn()` for methods, `filter` returns `[]` by default.
   - Add `CompendiumCollection` global (empty class stub -- type annotations only, real mock in Phase 3).
   - Add `TokenDocument` global (empty class stub).
   - Add `FilePicker` global with `browse: vi.fn().mockResolvedValue({ files: [] })`.
   - Include a header comment documenting which globals foundry-test-utils provides vs which this file provides, referencing the research gap table:
     ```
     // Missing from foundry-test-utils  | Used by           | Phase needed
     // -------------------------------- | ----------------- | -----------
     // game.packs (.filter, .get)       | CompendiumManager | Phase 3
     // CompendiumCollection             | Type annotations  | Phase 3
     // TokenDocument                    | TokenReplacer     | Phase 3
     // FilePicker                       | WildcardResolver  | Phase 3
     // canvas.tokens (.controlled)      | TokenReplacer     | Phase 3
     // SceneNavigation                  | Phase 5 (progress)| Phase 5
     ```
   - Guard each mock with `if (!globalThis.X)` / `if (typeof globalThis.X === "undefined")` checks to avoid overwriting foundry-test-utils stubs if they add them in future versions.

2. Create `tests/setup/cache-clearing.js` -- template file (NOT auto-executed, just a reference):
   - Export a documented `beforeEach` pattern showing how to clear caches.
   - Use comments explaining that each test file should import and adapt this pattern.
   - List all available clearCache methods:
     - `CompendiumManager.clearCache()` -- clears #indexCache, #wotcCompendiumsCache
     - `FolderManager.clearCache()` -- clears #importFolderCache
     - `WildcardResolver.clearCache()` -- clears #variantCache
     - `TokenReplacer.resetCounter()` -- resets #sequentialCounter
   - This file is NOT in vitest.config.js setupFiles (it is a reference template, not auto-run).

3. Create `tests/smoke.test.js` -- validates infrastructure works:
   - Test suite named "Test Infrastructure Smoke Test"
   - Test: "foundry-test-utils provides game global" -- assert `game` is defined, `game.settings` exists, `game.i18n` exists
   - Test: "foundry-test-utils provides ui global" -- assert `ui` is defined, `ui.notifications` exists
   - Test: "foundry-test-utils provides Hooks global" -- assert `Hooks` is defined, `typeof Hooks.on === 'function'`
   - Test: "project mocks provide game.packs" -- assert `game.packs` is defined, `typeof game.packs.filter === 'function'`, `typeof game.packs.get === 'function'`
   - Test: "project mocks provide FilePicker" -- assert `FilePicker` is defined, `typeof FilePicker.browse === 'function'`
   - Test: "Vitest globals are available" -- assert `describe`, `it`, `expect`, `vi` are all defined

4. Run `npm test` and verify it exits 0 with passing smoke tests.
  </action>
  <verify>
    <automated>cd /home/aiacos/workspace/FoundryVTT/token_updater && npm test 2>&1 | tail -20</automated>
  </verify>
  <done>`npm test` exits 0. All smoke tests pass (foundry-test-utils globals present, project mocks present, Vitest globals available). tests/setup/foundry-mocks.js provides game.packs, CompendiumCollection, TokenDocument, FilePicker stubs. tests/setup/cache-clearing.js provides documented beforeEach template.</done>
</task>

</tasks>

<verification>
Run the full verification sequence:
1. `npm test` -- exits 0, all smoke tests pass
2. `npm run test:coverage` -- exits 0, produces coverage output (0% is expected since no production code is tested yet)
3. Verify no peer dependency warnings in npm output
4. Verify `coverage/` directory is created but gitignored
</verification>

<success_criteria>
1. `npm test` exits 0 with Vitest output showing passing smoke tests
2. Vitest configured with jsdom, `unstubGlobals: true`, foundry-test-utils in setupFiles
3. beforeEach cache-clearing template exists in tests/setup/cache-clearing.js
4. Coverage infrastructure works (`npm run test:coverage` produces output)
5. Smoke test validates Foundry globals (game, ui, Hooks) and project mocks (game.packs, FilePicker) are functional
</success_criteria>

<output>
After completion, create `.planning/phases/01-test-infrastructure/01-01-SUMMARY.md`
</output>
